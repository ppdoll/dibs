-- ============================================================================
-- 이 파일은 더 이상 실행하지 않는다. **내용은 마이그레이션으로 옮겨졌다.**
--
--   → prisma/migrations/20260728020000_constraints/migration.sql
--
-- 왜 옮겼나
--   따로 두고 손으로 적용하던 동안 Prisma 는 이 객체들(CHECK·부분 유니크·트리거·
--   복합 FK·EXCLUDE)을 "설명되지 않는 드리프트"로 봤다. 그래서 스키마를 조금만
--   고쳐도 `migrate dev` 가 DB 리셋을 요구했고, 배포에서 이 단계를 한 번 잊으면
--   불변식이 통째로 빠진 채 서비스가 돌아갈 수 있었다.
--   이제 `prisma migrate deploy` 한 번이면 스키마와 불변식이 함께 올라간다.
--
-- 파일을 지우지 않고 남긴 이유
--   소스 곳곳의 주석이 "001_constraints.sql §10" 같은 식으로 이 이름을 가리킨다.
--   그 절 번호는 위 마이그레이션 파일 안에 그대로 살아 있다.
--
-- 제약을 새로 추가하려면
--   이 파일을 고치지 말고 **새 마이그레이션**을 만든다:
--     pnpm --filter @dibs/api exec prisma migrate dev --create-only --name add_xxx_constraint
--   그리고 생성된 migration.sql 에 DDL 을 직접 적는다.
-- ============================================================================
